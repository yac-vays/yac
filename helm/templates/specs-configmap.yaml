{{- if .Values.specs }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fullname" . }}-specs
  labels:
    app: {{ include "name" . }}
    chart: {{ include "chart" . }}
data:
  yac.yml: |
{{ .Values.specs | indent 4 }}
{{- end }}
